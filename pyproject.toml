[project]
name = "gwscan2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.2",
    "typer>=0.24.1",
    "zendriver>=0.15.3",
]

[project.scripts]
gwscan2 = "gwscan2.cli:app"

[tool.uv]
package = true